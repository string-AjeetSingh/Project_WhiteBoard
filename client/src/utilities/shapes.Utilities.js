const eventHandles = {
    useSelector: (selector, svgRef, svgElemRef, name) => {

        selector.current.select(svgRef, svgElemRef, name);

    }

}


function setSelector(theRef,) {

}








export { eventHandles }